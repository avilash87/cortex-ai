apiVersion: v1
kind: Service
metadata:
  name: cortex-console
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  selector:
    app: cortex-console
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
