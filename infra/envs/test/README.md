# test env

Root Terraform config for the `cortex-ai-test` HCP Terraform workspace.
Same modules as `dev`, promoted to here via a manual-approval CI gate
(Phase 5). Resource group: `rg-cortex-ai-test`. Tags: `owner`, `env=test`,
`cost-centre`.
