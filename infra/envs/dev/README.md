# dev env

Root Terraform config for the `cortex-ai-dev` HCP Terraform workspace.
Composes the modules under `infra/modules/` as they come online, phase by
phase. Resource group: `rg-cortex-ai-dev`. Tags: `owner`, `env=dev`,
`cost-centre`.
